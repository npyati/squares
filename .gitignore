# macOS system files
.DS_Store
**/.DS_Store

# Claude folder
.claude

# Optic is licensed, not redistributable, and is injected at deploy time from
# repository secrets. Rooted so it only covers this directory — one · at · a ·
# time's face is OFL and ships in the repo at oneatatime/fonts/.
/fonts/
